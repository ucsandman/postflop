[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[dev-dependencies]
rand = "0.8"
